import 'package:flutter/material.dart';

class All_songs extends StatelessWidget {
  const All_songs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('all songs'),
      ),
    );
  }
}
